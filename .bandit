skips:
  - B404  # subprocess import
  - B603  # subprocess without shell=true (we explicitly use shell=False)
  - B607  # partial executable path (we validate with shutil.which)

exclude_dirs:
  - dbt_packages
  - target
  - logs
  - transform
  - tests
  - node_modules
